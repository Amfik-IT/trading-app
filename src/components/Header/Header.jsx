import React from 'react';
import s from './Header.module.css';

const Header = () => {
    return (
        <div className={s.header}>
            <p>Привет</p>
        </div>
    );
};

export default Header;
